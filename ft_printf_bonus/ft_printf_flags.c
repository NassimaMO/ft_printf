#include <stdarg.h>
#include <stdio.h>
#include "ft_printf_bonus.h"

char *ft_find_flags(char *str, int *i, int *count)
{
    if (str[i] == '-')
    {
        // check the next number in str
        // see if the args is superior or equal than the number specified
        // add spaces to the right if not
    }
    if (str[i] == '0')
    {
        // check the next number in str
        // see if the args is superior or equal than the number specified
        // add spaces to the left if not
    }
    if (str[i] == '.')
    {
        // check the next number in str
        // see if the args is superior or equal than the number specified
        // add spaces to the left if not
    }
}